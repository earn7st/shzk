#include "SwapchainContext.h"

std::unique_ptr<SwapchainContext> CreateSwapchainContext(const VulkanContext& vulkanContext, uint32_t width, uint32_t height)
{

	return std::unique_ptr<SwapchainContext>();
}